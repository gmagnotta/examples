{
    "client-id" : "${env.OIDC_CLIENT_ID}",
    "provider-url" : "${env.OIDC_PROVIDER_URL}",
    "ssl-required" : "all",
     "credentials" : {
        "secret" : "${env.OIDC_CLIENT_SECRET}"
     }
  }